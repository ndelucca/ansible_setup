---
sway_config_dir: "{{ common_user_config_dir }}/sway"
sway_config_font: "{{ common_main_font }}"
sway_config_mod: Mod4

sway_default_terminal: wezterm
sway_font_name: "{{ common_main_font_name }}"
sway_font_size: "{{ common_main_font_size }}"
sway_status_dir: "{{ common_user_config_dir }}/i3status"

