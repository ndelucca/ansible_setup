---
- name: Installing NVM and Node.js
  hosts: my_machines
  become: true
  become_user: "{{ host_user.name }}"

  tasks:
    - name: Creating .bashrc_custom file
      ansible.builtin.file:
        path: "{{ host_user.home }}/.bashrc_custom"
        state: touch
        owner: "{{ host_user.name }}"
        group: "{{ host_user.group }}"
        mode: "0755"

    - name: Importing stephdewit.nvm role
      ansible.builtin.import_role:
        name: stephdewit.nvm
      vars:
        nvm_node_version: lts
        nvm_install_path: "{{ host_user.home }}/.nvm"
        nvm_shell_init_file: "{{ host_user.home }}/.bashrc_custom"
      environment:
        NVM_DIR: "{{ host_user.home }}/.nvm"
