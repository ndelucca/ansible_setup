---
- name: Linux mint setup
  hosts: my_machines
  become: true

  tasks:
    - name: Importing mint_setup git.yml tasks
      ansible.builtin.import_role:
        name: mint_setup
      vars:
        mint_setup_user: "{{ user.name }}"
        mint_setup_group: "{{ user.group }}"
        mint_setup_user_email: "{{ user.email }}"
        mint_setup_user_passwd: "{{ user.passwd }}"
